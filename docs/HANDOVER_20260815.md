# Devir notu — 2026-08-15

Bir önceki devir: `HANDOVER_20260813.md`. Bu belge onun yerini alır.

## Bir bakışta

| | |
|---|---|
| Sürüm | **1.0.8** (versionCode 9) |
| Test | **160 birim test, 0 hata** |
| Build | `assembleDebug`, `assembleRelease`, `bundleRelease` — üçü de başarılı |
| İmzalama | Var (keystore Boom Blocks'tan kopyalandı, `source/signing.properties`) |
| AdMob | **Gerçek kimlikler girildi**, `USE_TEST_IDS_IN_RELEASE = false` |
| Cihaz | **Samsung S8 bağlı, adb çalışıyor** |
| Git | ⚠️ **Baseline commit'te; 15 Ağustos'un tamamı commit EDİLMEDİ** |

## Ortamla ilgili en önemli değişiklik

**adb ve gerçek cihaz artık var.** 13-14 Ağustos boyunca "bu makinede cihaz
yok" varsayımıyla çalışıldı; bu artık geçersiz:

```
C:\Users\bhdre\AppData\Local\Android\Sdk\platform-tools\adb.exe
```

PATH'te değil, tam yolla çağrılır. `install -r`, `shell input tap`,
`exec-out screencap`, logcat denendi ve çalışıyor. **Pause tuşunun bozuk
olduğu ancak cihazda bulundu** — birim testler ve mockup'lar bunu
göremezdi. Bir davranışı iddia etmeden önce cihazda dene.

Emülatör hâlâ yok. Ses **dinlenmedi** (bu makinede hoparlör yok);
kulakla doğrulama proje sahibinde.

## Bugün ne değişti

**Oynanış**
- Trafik kendi hızıyla ilerliyor (eskiden asfaltla aynı hızda akıyordu,
  "park etmiş" görünüyordu). `TRAFFIC_SPEED_RATIO_MIN/MAX`.
- Bölüm **ancak üç görevin üçü de** tamamlanınca geçilir (`RunResult.passed`).
- İlk 8 bölümün öğrenme eğrisi yeniden tasarlandı (`LevelDef.trafficDensity`).
- Sonsuz modda **hız kilidi** (yalnız o modda).
- Reklamdan dönünce güvenli pencere: yol temizlenir, 3 sn dokunulmazlık,
  1.2 sn boyunca araç doğmaz.
- Yol deseninin frekansı düşürüldü (göz yorgunluğu şikâyeti).

**Arayüz**
- "Yıldız" kavramı **tamamen kaldırıldı** → görev yuvarlakları + yeşil tik.
- Bölüm haritası kerbli kıvrımlı **pist** (`ui/levels/TrackLayout.kt`,
  geometri saf Kotlin + 11 test); araç duraklar arasında kayıyor.
- İlk açılışta **dil seçim ekranı** (`ui/onboarding/`).
- HUD: üç hedefin kontrol listesi; hız göstergesi küçüldü ve yolun dışına
  çıktı; kontroller yuvarlak/kabartmalı 64 dp.
- **Pause z-sırası düzeltildi** (aşağıda).

**Araçlar**
- Çizimler baştan yapıldı: üstten bakışta arka yüz kısalıyor, stop
  lambaları ince şerit, alev katmanlı ve yumuşak.
- 7 gövde: Şehir 0 · Yarış Sedan 900 · Kuş SLX 1500 · Dağ Keçisi 1500 ·
  Kas Arabası 1800 · Boğa 67 2400 · Süper Araba 3200.
- **Özellik çarpanları** (son hız/ivme/fren/boost) + garajda dört çubuk.
- **Araca özel motor sesi ve korna** (`audio/CarSoundProfile.kt`,
  `audio/EngineVoice.kt`) — hâlâ tek bir ses DOSYASI yok, sentez.

**Yayın**
- Gerçek AdMob kimlikleri, gizlilik politikası + Data Safety + IARC
  dokümanları, ASO metinleri (TR/EN), mağaza başlıkları, yeni ikon.

## Cihazda bulunan hata (kayda değer)

Duraklat tuşu çalışmıyordu. Sebep: tam ekran "kaydırarak şerit değiştir"
katmanı HUD'ın **üstünde** çiziliyordu ve dokunuşu yutuyordu. Kontrol
butonları çalışıyordu çünkü onlar o katmandan sonra çiziliyordu. HUD en
üste alındı. Ders: dokunma sorunları yalnızca cihazda görünür.

## Sıradaki işler

1. **Commit + push** — günün tamamı sadece diskte (bu en acili).
2. ChatGPT'den gelen tepeden araç görselleri → `incoming/car_refs/`.
   Plan: **tint-mask** hattı (gövde magenta maske ile işaretli, çalışma
   anında seçili boyayla renklendirilir) → tek görselden 10 boya.
3. Tır + motosiklet: kendi çarpışma kutuları gerekiyor → "araç sınıfı"
   kavramı. F1: 5000 coin, en hızlı, V10 sesi sentezle.
4. Ekonominin ikinci yarısı: gelir/maliyet sabitleri (rapor `docs/BALANCE.md`
   ve ekonomi ajanının çıktısında hazır, uygulanmadı).
5. Ekran görüntüleri (sahibi çekecek, 8 kare listesi ASO raporunda),
   GitHub Pages'te gizlilik politikası, mağaza görsellerinin yeni araçla
   tazelenmesi.
6. Açık sorular: Boğa 67'nin nihai adı; Yarış Sedan'ın zayıf yönü yok.

## Nerede ne var

- Denge sabitleri: `game/GameConfig.kt`
- Araç verisi (geometri dahil): `game/CarCatalog.kt`
- Bölümler: `game/LevelCatalog.kt` · Pist geometrisi: `ui/levels/TrackLayout.kt`
- Ses: `audio/CarSoundProfile.kt` + `audio/EngineVoice.kt`
- Görsel üreticileri (Kotlin'in Python aynası): `docs/play_store_assets/tools/`
- Prototipten sapmalar: `PROVENANCE.md` (#1–#15)
- Karar geçmişi: `docs/CHANGELOG.md` · Denge gerekçeleri: `docs/BALANCE.md`

## ⚠️ Kesme anında havada kalan iş (2026-08-15 akşamı)

Oturum burada kapatıldı. Kapanırken **sprite entegrasyonu ajanı çalışıyordu**;
raporu alınamadı ama diske yazdıkları kalır. Yeni oturum şununla başlasın:

```
cd source && ./gradlew :app:testDebugUnitTest --offline
git status --short
```

- Testler geçiyor ve `git status` makul görünüyorsa iş tamamlanmıştır;
  `docs/CHANGELOG.md` ve `PROVENANCE.md` son girdilerine bakıp commit et.
- Testler kırıksa ya da yarım dosya varsa: `git diff` ile bak, gerekirse
  `git checkout -- <dosya>` ile geri al. Son sağlam commit: **10f36c2**
  (push edilmiş, kayıp riski yok).

**Yapılmakta olan iş:** `incoming/car_refs/` altındaki ChatGPT üretimi tepeden
araç görsellerini oyuna almak. Yöntem: her maskeden iki katman üretilir
(gövde = gri tonlamalı boyanabilir alan, detay = cam/lastik/far), çalışma
anında gövde seçili boyayla tint'lenip üstüne detay çizilir. Böylece tek
görselden 10 boya çıkar. Çarpışma kutusu değişmez; vektör çizim yolu
mağaza görselleri ve geri düşüş için korunur.

Sıradaki: taksi / motosiklet / tır / F1 dosyaları da klasörde ama bunlar
kendi çarpışma kutularını istiyor → "araç sınıfı" kavramı ayrı bir iş.
