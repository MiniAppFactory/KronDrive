# Devir notu — 2026-08-16

Bir önceki devir: `HANDOVER_20260815.md`. Bu belge onun yerini alır.

## Bir bakışta

| | |
|---|---|
| Sürüm | **1.0.9** (versionCode 10) |
| Test | **164 birim test, 0 hata** |
| Build | `assembleDebug`, `assembleRelease`, `bundleRelease` — üçü de başarılı |
| Git | ✅ **Temiz ve push edilmiş** — `28b70e4`, `origin/main` ile senkron |
| Cihaz | Samsung SM-G950F, **Android 7.0 / API 24** bağlı |
| Son test APK | `builds/KronDrive_test_2026-08-16_1642_v1.0.9.apk` |

## Bugün ne yapıldı

Günün tamamı **cihazda görülen** görsel kusurların düzeltilmesiydi. Beşi de
baseline'dan beri vardı; hiçbiri dünkü sprite işinin yan etkisi değildi.

1. **Şerit çizgileri** kesikli çizilmiyordu — `drawLine` + `PathEffect` bu
   cihazda (API 24) sessizce yok sayılıyor. Blok blok `drawRect`'e geçildi.
2. **Yol kenarı desenleri** (çim, köpük, bayrak, gece çizgileri, şehir
   ışıkları) **yukarı** akıyordu; beşinin de işareti çevrildi.
3. **Kerb** hiç kaymıyordu — sabit ızgaraya çizilip yerinde faz atlıyordu
   (~7.6 Hz stroboskop). Yapısal olarak düzeltildi **ve** hızı yola eşitlendi
   (çarpan 1/12 → 1.00, blok 46 → 50 px).
4. **Seyirci renk titremesi** — dünya koordinatına bağlandı, `%` → `mod`,
   `floor()` → `roundToInt()`.
5. **Kontrol tuşları** — emoji glifleri gitti, çizilmiş ikonlar geldi (dolu
   üçgen oklar, sarı şimşek, ampullü korna). Korna sağ sütunda yön okunun
   üstüne alındı. Araç gölgesi sprite yolunda kaldırıldı.
6. **Sprite ≥ hitbox** — Boğa 67'nin çizimi çarpışma kutusundan kenar başına
   0.21 dp dardı ("havaya çarptım" hatasının milimetrik hali). Düzeltildi,
   üretim betiğine öz-denetim eklendi.
7. **Garaj bilgisi** — kilitli araç kendi fabrika rengiyle önizleniyor;
   fabrika boyası için yanlış "satın al" satırı kalktı; **seviye kilidi artık
   fiyatı gizlemiyor** ("Sv 2 · 1500").

Tam liste ve gerekçeler: `PROVENANCE.md` #18–#23, `docs/CHANGELOG.md`
2026-08-16 başlığı.

## Ölçüm disiplini — bunu okumadan performans ölçme

**Tema her koşuda rastgele seçiliyor** (`GameEngine.kt`, `RoadTheme.entries`)
ve temalar arasında kare başına çizim çağrısı **~185 ile ~780 arasında, 4 kat**
değişiyor. Temayı sabitlemeden yapılan her A/B ölçümü gürültüdür.

Bu yüzden 15 Ağustos'taki "sprite performans regresyonu yok" iddiası **geri
çekildi** (bkz. PROVENANCE #16 düzeltme kutusu). Sprite kararı mimari
gerekçeyle duruyor ama temiz bir ölçüm **yok**.

Ölçerken temayı geçici olarak sabitle, sonra geri al.

## Cihazda çalışan yöntemler (tekrar keşfetme)

- adb PATH'te değil: `AppData\Local\Android\Sdk\platform-tools\adb.exe`
- `input tap` ekran geçişlerinden sonra **yutuluyor**; uzun basış kullan:
  `shell input swipe X Y X Y 140`
- Oyuna gitme: cold start → 4 sn → `(540,890)` KARİYER → 2 sn →
  `(407,1720)` Bölüm 1 → 2 sn → `(540,1668)` BAŞLA
- Git Bash `/sdcard/...` yolunu Windows yoluna çeviriyor →
  `export MSYS_NO_PATHCONV=1`
- **`screenrecord` açıkken dokunuşlar işlenmiyor** — önce koşuya gir, sonra
  kaydı başlat.
- Akış yönü ölçümü ekran görüntüsüyle YAPILAMAZ (bir saniyede desen periyodunu
  aşıyor). `screenrecord` + `ffmpeg fps=20` ile komşu kare çıkar, sütun
  profilini çapraz korelasyonla kaydır.

## Sıradaki iş — karar bekleyen tek madde

**Reklam sayacı kaçağı** (bugün bulundu, HENÜZ YAPILMADI). Geçiş reklamı
sayacı yalnızca `stats.completed` iken artıyor (`KronViewModel.onRunFinished`).
Yani çarpıp "ana ekrana dön" → bölümü tekrar seç döngüsü **sınırsız ve
reklamsız**. `INTERSTITIAL_EVERY_N_RETRIES = 2` sabiti var ama
**kullanılmıyor** (14 Ağustos'ta "TEKRAR" butonu kaldırılmış).

Önerilen paket (sahibine sunuldu, onay bekliyor):
1. Sayaç, **10 saniyeyi geçen her kariyer koşusunda** artsın (başarı şartı
   kalksın).
2. Sıklık **2 → 3** (mevcut oyuncunun hissi bozulmasın).
3. **İlk 4 bölümde geçiş reklamı hiç gösterilmesin** — düzeltme tek başına
   yapılırsa en çok zorlanan oyuncu en çok reklam gören kesime dönüşür ve
   erken hunide terk artar.

## Açık işler

- **Sahibinden bekleniyor:** GitHub Pages'i aç (`main` / `/docs`) — gizlilik
  politikası metinleri hazır ama site 404 veriyor, Play gönderimi için engel.
  AdMob konsolunda **consent formu tanımlı değil** (logcat kanıtı var), AB/UK
  için engel. Uyum ajanının 6 sorusu: `docs/PLAY_RELEASE_CHECKLIST.md`.
- **Ekran görüntüleri** — sahibi çekecek. Plan: `docs/play_store_assets/SCREENSHOT_PLAN.md`.
  ⚠ S8'in ham çıktısı 1440×2960, Play oranına uymuyor; 1080×1920'ye kırpılmalı.
- **Ekonomi** — analiz yapıldı, uygulanmadı. Üç yapısal sorun: pasif gelir
  (günlük 900 + reklam 750 = 1650/gün) oynanış gelirini (~95/bölüm) eziyor;
  seviye kapısı bağlamıyor (3 koşuda seviye 2, ama 1500 coin çok daha uzun);
  yükseltmeler 28.000 vs tüm araçlar 11.300. Öneriler bu oturumun sonunda
  konuşuldu, yazıya geçirilmedi.
- **Performans** — ~40 FPS (medyan 24-27 ms). Bu oturumda recomposition
  daraltıldı ve çizim çağrıları birleştirildi ama **ölçülmedi**.
- **Araç sınıfı** (tır/motosiklet/F1) — tasarım hazır: `docs/VEHICLE_CLASSES.md`.
  Kod yazılmadı.
- **FUN GATE** — `docs/FUN_GATE_v1.0.9.md` (ajan yarıda kesildi, eksik olabilir).

## Nerede ne var

Denge sabitleri `game/GameConfig.kt` · Araç verisi `game/CarCatalog.kt` ·
Sprite kayıt/yükleme `ui/common/CarSprites.kt` · Çizim `ui/common/CarArtwork.kt` ·
Sahne `ui/game/GameRenderer.kt` · Sprite üreticisi `tools/build_car_sprites.py` ·
Mağaza görseli üreticileri `docs/play_store_assets/tools/`
