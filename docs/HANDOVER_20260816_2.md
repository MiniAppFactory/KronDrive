# Devir notu — 2026-08-16 (akşam oturumu)

Bir önceki devir: `HANDOVER_20260816.md`. **O belge geçerliliğini korur** —
bu belge onun yerini almaz, üzerine ekler. Gün içindeki görsel düzeltmeler,
cihaz yöntemleri ve ölçüm disiplini hâlâ orada anlatılıyor; burada yalnızca
akşam oturumunda olanlar var.

Oturum **proje sahibi tarafından duraklatıldı.** Yarım kalan tek iş
performans ölçümü; nerede kaldığı ve nasıl devam edileceği aşağıda.

## Bir bakışta

| | |
|---|---|
| Sürüm | 1.0.9 (versionCode 10) — değişmedi |
| Test | **177 birim test, 0 hata** (sabah 164'tü) |
| Build | `testDebugUnitTest` + `assembleDebug` + `assembleRelease` başarılı |
| Git | ✅ Temiz ve push edilmiş — `50eb4d6`, `origin/main` ile senkron |
| Release APK | 4.27 MB, imzalı |
| Son test APK | `builds/KronDrive_test_2026-08-16_1900_v1.0.9.apk` (sahibine gönderildi) |

## Bu oturumda ne bitti

### 1. Reklam sayacı kaçağı kapatıldı — `d3f2d28`

Sabahki devir notunun "sıradaki iş" maddesiydi, sahibi **tam paketi**
onayladı ve uygulandı:

1. Sayaç 10 saniyeyi geçen **her** kariyer koşusunda artar, başarı şartı yok
   (`INTERSTITIAL_MIN_RUN_SECONDS = 10`)
2. `INTERSTITIAL_EVERY_N_LEVELS` **2 → 3**
3. İlk 4 bölüm reklamsız (`INTERSTITIAL_FREE_LEVELS = 4`)

Karar `game/AdFrequency.kt`'e **saf Kotlin** olarak taşındı, 13 JVM testiyle
kilitlendi. Cihazda doğrulandı (DataStore `run-as` ile doğrudan okundu):
5 sn'lik çarpan koşu sayacı **artırmadı** (0→0), ~20 sn'lik çarpan koşu
**artırdı** (0→1). Ayrıntı: `CHANGELOG.md` "2026-08-16 (2)", `PROVENANCE.md` #24.

### 2. Ortak keystore kararı yazıya geçti — `8cfa323`, `50eb4d6`

Release, sertifika CN'i **`Blast the Blocks`** olan ortak keystore ile
imzalanıyor. Sahibi bunu **bilerek** böyle istiyor; Kron Drive'a ayrı keystore
üretilmeyecek. **Bu bir daha sorulmaz.**

Aynı yerde bir düzeltme yapıldı: checklist "keystore kaybolursa güncelleme bir
daha yayınlanamaz" diyordu. Alias `UPLOAD` olduğu için bu bir **upload**
anahtarı; Play App Signing'de asıl anahtarı Google tutar ve upload anahtarı
sıfırlatılabilir. ⚠ Ancak **Play konsolunda Play App Signing kaydının açık
olduğu doğrulanmadı** — çıkarım dosya adına ve alias'a dayanıyor. Kayıt yoksa
eski uyarı geçerli.

**S-6 kapandı:** sahibi anahtarları yedeklediğini bildirdi. Beyandır,
teknik doğrulama yapılmadı. Sahibinden bekleyen madde 6 → **5**.

### 3. Ekonomi denge önerisi hazır — UYGULANMADI

`docs/ECONOMY_BALANCE_PROPOSAL.md` (673 satır, **henüz commit edilmedi**).
Üç iddianın akıbeti:

- **Pasif gelir oynanışı eziyor — DOĞRU**, teşhis düzeltildi: günlük görev
  pasif değil, oynanıyor; gerçekten pasif olan tek şey ödüllü reklam. En sert
  sayı: 30 bölümlük kariyerin tamamı ilk kez geçilerek ~4.050 coin veriyor.
- **Seviye kapısı bağlamıyor — DOĞRU, sanılandan kötü.** Sapma sistematik:
  kapı her seferinde gereken paranın **~%40'ında** açılıyor. En yüksek seviye
  şartı 6 ve oyuncu ona 9. bölümde ulaşıyor → kalan 21 bölümde
  `requiredCarLevel` dekoratif.
- **Yükseltmeler orantısız pahalı — sayılar doğru (28.000 / 11.300), teşhis
  yanlış.** Bu `BALANCE.md:291-293`'te gerekçeli bilinçli bir karar. Fiyat
  değişikliği önerilmiyor. Oradaki gerçek sorun başka: maliyet doğrusal, etki
  dışbükey — ilk satın alma 250 coin karşılığı **+1 km/h**.

**Sahibi onayladı: "önce ölç, sonra uygula".** Yani sıradaki adım:
`LevelCurveTest` içindeki `olcum dokumu` testini çalıştırıp koşu başına
**gerçek** coin/skor sayılarını almak, öneriyi bu sayılarla teyit etmek,
sonra uygulamak. Uygulanacak olan (onaylı):

| Sabit | Yer | Şu an | Olacak |
|---|---|---|---|
| `TIER_REWARDS` | `DailyChallengeGenerator.kt:86` | 120/260/520 | **80/140/280** |
| `SCORE_PER_BONUS_COIN` | `GameConfig.kt:390` | 120 | **70** |

**Öneri 3 (`XP_PER_CAR_LEVEL` 500 → 1250) BU TURDA YOK** — sahibi kapsam
dışında bıraktı. Uygulanacaksa tek seferlik XP migrasyonu (`yeniXp = eskiXp *
2.5`) zorunlu, yoksa mevcut oyuncular seviye kaybeder.

Ayrıca belgede duran, kapsam dışı ama gerçek iki bulgu:
- **Çift Ödül booster'ı (300 coin) matematiksel olarak hiç kâra geçmiyor** —
  iyi bir koşu ~166 coin ödüyor, ikiye katlayınca net −134.
- **`docs/BALANCE.md` üç yerde kodla çelişiyor** (günlük ödül 400–500 vs 900;
  reklam "her 2 bölümde" vs 3 + ilk 4 bedava; yükseltme eğrisi doğrusal
  gösteriliyor, kod dışbükey). Bu belge "denge nereden geliyor" sorusunun
  cevabı olarak kullanılıyor — yanlış kalması sonraki kararları zehirler.

## YARIM KALAN: performans ölçümü

Ajan tam ölçüme başlarken (probe build'i kuracakken) durduruldu. **Hiçbir
ölçüm sonucu yok** — bugün için FPS'e dair yeni bir sayı YOK. Elimizdeki tek
rakam hâlâ sabahki devir notundaki ~40 FPS (medyan 24-27 ms) ve o da
2026-08-16 sabahından, sonrasında yapılan recomposition daraltması
ölçülmedi.

Ajan kaynak ağacına **geçici ölçüm kancaları** bırakmıştı. Bunlar depodan
temizlendi ve yama olarak saklandı:

```
backups/perf_probe_20260816.patch     (294 satır)
```

Yama şunları içeriyor:
- `game/PerfProbe.kt` (yeni) — tema ve çizim katmanı maskesi için statik alanlar
- `MainActivity.kt` — intent extra'sından `perftheme` / `perfmask` okuma
- `GameEngine.kt` — rastgele tema seçimini `PerfProbe.forcedTheme` ile
  geçersiz kılma
- `GameRenderer.kt` — çizim katmanlarını maskeyle kapatabilme
- `TempEngineStepBenchTest.kt` (yeni) — motor adımının JVM tarafı ölçümü

Devam etmek için: `git apply backups/perf_probe_20260816.patch`

**Ölçüme başlamadan önce sabahki devir notundaki "Ölçüm disiplini" bölümünü
oku.** Özeti: tema her koşuda rastgele ve temalar arası çizim yükü **4 kat**
değişiyor; temayı sabitlemeden yapılan her A/B ölçümü gürültüdür. 15
Ağustos'taki "sprite regresyonu yok" iddiası tam da bu yüzden geri çekildi.

Ölçüm bitince **yamayı geri al** — bu kancalar release'e girmemeli.

## Sahibi kararı: ChatGPT görselleri KULLANILMIYOR

`incoming/Kron Drive Play store görseller chat gpt/` altında 12 görsel var
(7 oyun içi mockup 941×1672, 1 feature graphic 1794×876, 4 buton varlığı).
Görsel olarak iyiler ama **oyunu yansıtmıyorlar** — sahibi kararı, tartışma
kapandı.

Mockup'ta olup oyunda olmayanlar: yıldızlar (arayüzden bilerek kaldırıldı,
yerine görev + yeşil tik), enerji kapısı (`8/10 · REFILL IN 05:30`),
leaderboard (sunucu/hesap yok), EVENTS/SHOP sekmeleri, yakıt göstergesi
(bizde boost enerjisi var), "Boga 67" yazımı, 3/4 perspektifte 3D render
araçlar (bizimki tepeden sprite).

Feature graphic (`(8)`) teknik olarak kullanılabilirdi — oranı **tam 2.048**,
gerçek logomuzu ve doğru alt başlığı taşıyor — ama aynı tutarlılık gerekçesiyle
o da kullanılmıyor: feature graphic'te 3D araçlar, ekran görüntülerinde
sprite'lar olursa mağaza sayfası kendi içinde çelişir.

**Bunların değeri hedef tahtası olarak.** Sahibi "oyun içi ekranlar böyle olsa
güzel olurdu" dedi. Ucuz olanlar: Perfect Dodge/combo bandı (veri zaten var,
çizilmiyor), fren/boost düğmelerine renkli halka parlaması, HUD'un etiketli
kutucuk şeridine dönmesi, arka planda hız çizgileri. Pahalı olanlar: 3D render
araçlar (sprite'ları baştan üretmek + sprite ≥ çarpışma kutusu denetimi),
izometrik bölüm haritası.

⚠ **Görsel yükseltme ölçülerek yapılmalı.** Oyun zaten ~40 FPS; Compose'da
parlama/bulanıklık API 24'te pahalı. Performans ölçümü bitmeden HUD'a efekt
eklemek, elimizdeki tek temiz ölçüm fırsatını da yakar.

## Mağaza görsel paketi (sahibine gönderildi)

claude.ai sohbetine yüklenmek üzere kendi kendine yeten bir paket üretildi:
`KronDrive_store_gorsel_paketi.zip` (2.87 MB, 46 dosya). Üretici betikler +
oyunun gerçek sprite'ları + çekim planı + README.

Üç betiğin yolu pakete göre düzeltildi (`sprite_car.py`, `gen_feature.py`,
`gen_launcher.py` depo ağacına uzanıyordu). **Düzeltmeler yalnızca paket
kopyalarında — depodaki dosyalara dokunulmadı.** Paketteki dört üretici
çalıştırılıp doğrulandı.

README'de Play kuralı yazılı: ikon ve feature graphic serbestçe
tasarlanabilir, **ekran görüntüleri cihazdan çekilmek zorunda** (uygulamada
olmayan arayüzü ekran görüntüsü diye koymak yanıltıcı mağaza girişidir).
Gerçek çekimin üstüne başlık/çerçeve eklemek serbest — `caption_screenshot.py`
bunu yapıyor.

## YENİ İŞ (sahibi verdi, başlanmadı): "Beety" aracı

Sahibi yeni bir araç referansı ekledi ve **en pahalı araç olmasını** istedi.

**Referans:** `incoming/car_refs/beety.png` + `beety_mask.png`. Tepeden
görünüm, sarı, klasik "vosvos" silueti — yuvarlak gövde, ayrık çamurluklar,
arka kaput havalandırma çizgileri, krom tampon.

**Fiyat: 4000 coin — sahibi kararı, 2026-08-16.** Bugünkü en pahalı araç
(Süper Araba 3200) geçildi. Not: `VEHICLE_CLASSES.md`'deki F1 taslağı 5000
coin; F1 ileride o fiyatla eklenirse Beety en pahalı olmaktan çıkar. Bu
bilinerek bırakıldı, o gün tekrar bakılır.

**`requiredCarLevel`** kararı verilmedi. En pahalı araç olduğu için seviye
şartı da gözden geçirilmeli — şu an oyundaki en yüksek şart 6 ve oyuncu ona
9. bölümde ulaşıyor, yani seviye kapısı 4000'lik bir araç için anlamlı bir
engel oluşturmuyor (bkz. ekonomi önerisinin ikinci bulgusu).

⚠ **Bu iş ekonomi işiyle çakışıyor.** `ECONOMY_BALANCE_PROPOSAL.md` içindeki
tüm hesaplar "tüm araçlar toplamı **11.300** coin" varsayımına dayanıyor.
4000'lik bir araç eklenince bu 15.300 olur ve önerinin ilerleme eğrisi
tahminleri (kaç günde bitirilir) yeniden hesaplanmalı. Sıralama önemli:
**önce ekonomi 1+2 uygulanıp ölçülmeli, sonra araç eklenmeli** — yoksa iki
değişikliğin etkisi birbirine karışır ve hiçbiri doğrulanamaz.

**Yapılacaklar (hiçbiri başlanmadı):**
1. Sprite üretimi — `tools/build_car_sprites.py` ile iki katman (gövde +
   detay). Betikteki **sprite ≥ çarpışma kutusu öz-denetimi** geçmeli
   (16 Ağustos'ta Boğa 67'de 0.21 dp'lik sapma bu denetimle bulunmuştu).
2. `game/CarCatalog.kt` — araç verisi, vektör geri düşüş geometrisi ve
   **fabrika boyası**. Referans sarı; kural gereği gövdeye sahip olmak
   fabrika boyasını da açar.
3. Fiyat + `requiredCarLevel` — en pahalı araç olduğu için seviye şartı da
   gözden geçirilmeli (şu an en yüksek şart 6 ve 9. bölümde doluyor, yani
   seviye kapısı bu araç için anlamlı bir engel oluşturmuyor).
4. `AppLanguage.pick()` ile TR/EN ad.
5. Testler: `CarCatalogTest` fiyat/sıralama beklentileri güncellenmeli.
6. Garaj ekranında önizleme ve mağaza görsellerinde kadro (`car_lineup.py`
   7 araç varsayıyor → 8 olacak).

⚠ **Marka riski — karar sahibinin.** Bu siluet belirli bir gerçek otomobile
(klasik VW Beetle) çok yakın ve o gövde biçimi otomotiv tasarımları arasında
en tanınır olanlardan biri. Projenin mevcut kuralı zaten "marka adı
kullanılmaz" (Boğa 67, Kuş SLX, Dağ Keçisi adları bu yüzden uyduruldu).
İsim tarafı sorun değil — "Beety" bir marka adı değil. Asıl soru **gövde
biçiminin** ne kadar birebir kalacağı. Öneri: oranları biraz kaydırıp
"klasik böcek arabası" arketipinde kalmak, referansın birebir kopyası
olmamak. Bu bir hukuk görüşü değil, dikkat notudur.

## Açık işler

**Sahibinden bekleniyor (5 madde, `PLAY_RELEASE_CHECKLIST.md` §S):**
- **S-1** hedef kitle yaş grubu (13+ önerisi hazır; çocuklar dâhil edilirse
  TFCD/TFUA kodda set edilmeli ve **yeni AAB** gerekir)
- **S-2** Play hesabı kişisel mi kuruluş mu (kapalı test şartını bu belirler)
- **S-3** AdMob GDPR mesajı yayımlanmamış — logcat kanıtı var, AB/UK için engel
- **S-4** dağıtım ülkeleri ve fiyat (ücretsiz seçilirse geri dönüşü yok)
- **S-5** app-ads.txt doğrulaması tetiklendi mi
- Ayrıca: **GitHub Pages kapalı** (`main` / `/docs`) — gizlilik politikası
  metni hazır ama site 404, Play gönderimi için engel
- **Play App Signing kaydı açık mı** — yukarıdaki düzeltme buna dayanıyor

**Kod tarafı:**
- **Ekonomi 1+2** — onaylı, ölçüm sonrası uygulanacak (yukarıda tablo)
- **Performans** — ölçüm yarım, yama `backups/` altında
- **Ekran görüntüleri** — çekilmedi. Plan: `docs/play_store_assets/SCREENSHOT_PLAN.md`.
  ⚠ S8 ham çıktısı 1440×2960, 1080×1920'ye kırpılmalı. *(Ben cihazdan
  çekebilirim — bugün adb ile oyunu sürüp kare aldım, plandaki sahnelere
  girmek mümkün.)*
- **Girmemiş araçlar — 6 referans hazır bekliyor.** Tasarım `docs/VEHICLE_CLASSES.md`
  içinde **ölçülmüş** durumda, kod yok. Ayrıntı aşağıdaki bölümde.
- **`BALANCE.md` kodla çelişiyor** — üç yer, yukarıda
- **Çift Ödül booster'ı zararda** — 300 coin, hiç kâra geçmiyor
- **FUN GATE** — `docs/FUN_GATE_v1.0.9.md` (ajan yarıda kesilmişti, eksik olabilir)
- **A-9 küçük:** `ui/settings/SettingsScreen.kt:233` hâlâ "Sürüm 1.0.0" yazıyor

## Oyuna girmemiş araçlar — referanslar hazır, kod yok

**Oyunda olan 7 satılık araç:** Şehir 0 · Yarış Sedan 900 · Kuş SLX 1500 ·
Dağ Keçisi 1500 · Kas Arabası 1800 · Boğa 67 2400 · Süper Araba 3200
(+ satılık olmayan trafik aracı).

**Referansı hazır ama oyunda OLMAYAN 6 araç** (`incoming/car_refs/`, her
birinin `_mask` dosyası da var). `docs/VEHICLE_CLASSES.md` §"Bugünkü
referansların sınıflara dağılımı" bunları **ölçmüş** — aşağıdaki oran ve
uyarılar oradan geliyor, tahmin değil:

| Referans | Oran | Sınıf | Durum |
|---|---|---|---|
| `06_taksi` | 0.493 | `BINEK` | **Trafik olarak ideal** — obstacle paletinde zaten sarı (`FFD60A`) var, taksi o rengin gerekçesi olur |
| `05_beyaz_sedan` | 0.490 | `BINEK` | Trafik çeşitliliği. Oyuncuya verilirse `COLOR_GLACIER` çakışması gözden geçirilmeli |
| `10_f1` | 0.473 | `BINEK` | **Ayrı sınıf GEREKMİYOR** — aşağıya bak |
| `11_station_wagon` | 0.434 | **hiçbiri** | `BINEK`'e sokulursa görsel 26.40 dp < hitbox 28.16 dp → kenar başına **−0.88 dp havaya çarpma**. Olduğu gibi alınmamalı |
| `07_motosiklet` | 0.375 | `MOTOSIKLET` | Kendi kutusunu istiyor |
| `09_tir` | 0.237 | `AGIR` | Tek dikey referans (tuval zaten 2:1) |

### Maliyet iki kademeli — bu ayrım işi planlarken önemli

- **Ucuz olanlar** (`06_taksi`, `05_beyaz_sedan`, `10_f1`): mevcut `BINEK`
  kutusuna oturuyorlar. Yeni çarpışma sınıfı, yeni test, yeni denge ekseni
  gerekmiyor — sprite üret, katalog kaydı aç, bitir.
- **Pahalı olanlar** (`07_motosiklet`, `09_tir`, `11_station_wagon`): kendi
  çarpışma kutularını istiyorlar. Belge bunun perfect-dodge penceresini
  bozduğunu ve düzeltilmesi gerektiğini yazıyor (§"Perfect dodge penceresi
  bozulur"). Bu, kutunun **beşinci bir denge ekseni** hâline gelmesi demek.

### F1 hakkında: ölçüm sahibinin beklentisiyle çelişiyor

Sahibinin notu "F1 daha geniş" demiş. **Referans çizim bunu söylemiyor:**
`10_f1` oranı 0.473, Şehir'inki 0.518 — yani elimizdeki F1 sanatı otomobilden
*daha dar*. Belge üç yol sunuyor ve **(A) `BINEK`'te bırakmayı öneriyor**
(sprite 216×456'ya oturur, hitbox'a pay +0.32 dp — bugün yayında olan Kas
Arabası'ndan daha iyi; sıfır risk, hiçbir test değişmiyor). Sahibi "daha büyük
olsun"da ısrar ederse doğru yol (B) `FORMULA` sınıfı (40×85), ama bu dikey
örtüşmeyi %5.9 artırır — yani F1 kimliği bir **bedelle** gelir. (C) kutuyu
yanlamasına genişletmek belgede **reddedilmiş** ("kutu büyümez" kuralı
2026-08-13 adalet düzeltmesinin bekçisi).

**F1 fiyatı `VEHICLE_CLASSES.md`'de 5000 coin, "sahibi kararı" olarak yazılı**
— Beety'nin fiyatı bununla çelişiyor, yukarıdaki Beety bölümüne bak.

### Kanonik referans klasörü: `Kron-Drive/incoming/car_refs/`

**Sahibi kararı, 2026-08-16 — bir daha sorulmaz.** Bütün araç referansları
proje içindeki bu klasörde bulunur (32 dosya: 16 referans + maskeleri +
README). Beety oraya eklendi, klasör tam.

`APPDeveloper/incoming/car_refs/` (üst seviye) artık **kanonik değil**.
İçinde `03_kas_arabasi_massk.png` diye yazım hatalı bir dosya duruyor ve
README'si yok. Silinmedi — sahibinin dosyaları, dokunulmadı; ama yeni
referans oraya değil proje klasörüne konur.

Bu ayrım bir kez karışıklık yarattı: beety önce yalnızca üst seviyedekine
konduğu için proje klasöründe bulunamadı.

## Cihazda çalışan yöntemler (bu oturumda eklenenler)

Sabahki devir notundaki liste geçerli. Yeni öğrenilenler:

- **Cihaz kilitli olabilir** — kilitliyken ekran görüntüsü alırsan kilit
  ekranını ölçersin. Uyandır: `shell input keyevent 224`, sonra
  `shell input swipe 540 1800 540 700 200`
- **DataStore'u doğrudan okuma** (debug build'de):
  `adb exec-out run-as com.miniappfactory.krondrive cat
  files/datastore/kron_drive_progress.preferences_pb` → ikili proto; anahtar
  adından sonraki varint değerdir. Reklam sayacı bu yolla doğrulandı.
- Koşu içi kontroller (1080×2220): sol ok `(142,1839)`, sağ ok `(935,1839)`,
  FREN `(142,2081)`, nitro `(935,2081)`, duraklat `(540,157)`.
  **Hayatta kalmak için FREN'i basılı tutmak işe yarıyor:**
  `shell input swipe 142 2081 142 2081 12000`
- Çarpma diyaloğu: "SONUÇLARI GÖR" `(540,1350)`, sonuç ekranı "ANA MENÜ"
  `(540,1738)`

## Bilinen, düzeltilmeyen

**Duraklatmadan çıkışta reklam sayacı bir koşu gecikiyor.**
`PausedOverlay.onQuit` sayaç artışını DataStore'a asenkron yazarken reklam
kararını hemen okuyor, yani o çıkışta sayacın bir önceki değeri görülüyor.
Sayaç kalıcı olduğu için reklam kaybolmuyor, sadece bir koşu sonraya kayıyor.
Sonuç ekranından çıkışta sorun yok (oyuncu saniyeler sonra dokunuyor).
